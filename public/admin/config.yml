backend:
  name: github
  repo: GDK-13/shaggy-solstice
  branch: main

media_folder: "public/media" # Onde o Decap vai salvar as imagens 
public_folder: "media" # O texto que o Decap vai escrever no arquivo

collections:
  - name: "projetos"
    label: "Ações e Projetos"
    folder: "src/content/projetos"
    create: true
    slug: "{{slug}}"
    extension: "json"
    format: "json"
    fields:
      - {label: "Título", name: "title", widget: "string"}
      - {label: "Descrição", name: "description", widget: "text"}
      - {label: "Imagem", name: "image", widget: "image", required: false}